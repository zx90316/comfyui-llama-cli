# ComfyUI LLAMA-CLI 功能實體清單

## 節點清單

| 節點 | 顯示名稱 | 類別 | 二進位 | 說明 |
|------|----------|------|--------|------|
| LlamaCliConfig | Llama CLI Config | AI/LlamaCpp | - | 配置 llama.cpp 二進位路徑、模型、GPU、快取、推測解碼等共用設定 |
| LlamaGenerate | Llama Generate | AI/LlamaCpp | llama-cli | 文字補全 / 文字生成 |
| LlamaChat | Llama Chat | AI/LlamaCpp | llama-cli | 多輪對話（Jinja 模板 / conversation 模式）|
| LlamaEmbedding | Llama Embedding | AI/LlamaCpp | llama-embedding | 文字嵌入向量 |
| LlamaInfill | Llama Infill | AI/LlamaCpp | llama-cli | 程式碼填充（Fill-in-the-Middle）|
| LlamaImageEncoder | Llama Image Encoder | AI/LlamaCpp | - | 將 ComfyUI IMAGE tensor 轉為圖片檔供多模態使用 |
| LlamaProcessMonitor | Llama Process Monitor | AI/LlamaCpp | - | 查看 / 釋放 keep-alive 進程 |

---

## 功能分類

### 1. 伺服器 / 進程管理

| 功能 | 說明 |
|------|------|
| 二進位路徑設定 | 指定 llama.cpp 編譯目錄，自動偵測可用執行檔 |
| GGUF 模型選擇 | 透過 ComfyUI folder_paths 下拉選單選取模型 |
| One-shot 模式 | subprocess.run 執行，完成後自動釋放 VRAM |
| Keep-alive 模式 | subprocess.Popen + interactive mode，保持模型常駐 |
| 進程監控 | 查看執行中進程的 PID、模型名、存活時間 |
| 手動釋放 | 透過 ProcessMonitor 節點終止指定進程 |
| 自動清理 | ComfyUI 關閉時透過 atexit 終止所有子進程 |

### 2. GPU 與記憶體設定

| 功能 | CLI 參數 | 說明 |
|------|----------|------|
| GPU 層數 | `-ngl` | 設定載入至 GPU 的層數（99=全部）|
| 上下文長度 | `-c` | 設定上下文視窗大小 |
| Flash Attention | `-fa` | 啟用 Flash Attention 加速 |
| GPU 分割模式 | `--split-mode` | none / layer / row |
| 主 GPU 選擇 | `--main-gpu` | 指定主要 GPU 編號 |
| CPU 執行緒 | `-t` | 設定 CPU 計算執行緒數 |
| 並行序列數 | `-np` | 設定並行處理序列數 |

### 3. KV Cache 設定

| 功能 | CLI 參數 | 說明 |
|------|----------|------|
| K Cache 型別 | `--cache-type-k` | f16/f32/bf16/q8_0/q4_0/q4_1/iq4_nl/q5_0/q5_1 |
| V Cache 型別 | `--cache-type-v` | 同上 |

### 4. 推測解碼（Speculative Decoding）

| 功能 | CLI 參數 | 說明 |
|------|----------|------|
| 推測類型 | `--spec-type` | none/draft-simple/draft-eagle3/draft-mtp/ngram-* |
| 最大推測 token | `--spec-draft-n-max` | 每步最大推測 token 數 |
| Draft 模型 | `--model-draft` | 推測解碼用的輕量模型 |

### 5. 模板與格式

| 功能 | CLI 參數 | 說明 |
|------|----------|------|
| Jinja 模板 | `--jinja` | 啟用 Jinja 聊天模板引擎 |
| 聊天模板 | `--chat-template` | 指定聊天模板名稱 |

### 6. 視覺 / 多模態

| 功能 | CLI 參數 | 說明 |
|------|----------|------|
| 多模態投射器 | `--mmproj` | 指定 mmproj GGUF 檔案路徑 |
| 投射器 GPU 卸載 | `--mmproj-offload` | 啟用/停用投射器 GPU 加速 |
| 圖片輸入 | `--image` | 傳入圖片檔案供視覺模型處理 |
| ComfyUI 圖片整合 | LlamaImageEncoder | 將 IMAGE tensor 自動轉為圖片檔 |

### 7. 文字生成（LlamaGenerate）

| 功能 | CLI 參數 | 說明 |
|------|----------|------|
| 提示詞輸入 | `-p` | 文字生成的提示詞 |
| 生成長度 | `-n` | 最大生成 token 數 |
| 溫度 | `--temp` | 取樣溫度控制隨機性 |
| Top-K | `--top-k` | Top-K 取樣 |
| Top-P | `--top-p` | Nucleus 取樣 |
| Min-P | `--min-p` | Min-P 取樣 |
| 隨機種子 | `-s` | 固定隨機種子以重現結果 |
| 重複懲罰 | `--repeat-penalty` | 減少重複內容 |
| 重複視窗 | `--repeat-last-n` | 重複懲罰的回溯範圍 |
| 存在懲罰 | `--presence-penalty` | OpenAI 風格存在懲罰 |
| 頻率懲罰 | `--frequency-penalty` | OpenAI 風格頻率懲罰 |
| Typical-P | `--typical` | Locally Typical 取樣 |
| 動態溫度範圍 | `--dynatemp-range` | Dynamic Temperature 範圍 |
| 動態溫度指數 | `--dynatemp-exp` | Dynamic Temperature 指數 |
| Mirostat | `--mirostat` | Mirostat 取樣（0/1/2）|
| Mirostat Tau | `--mirostat-tau` | Mirostat 目標熵值 |
| Mirostat Eta | `--mirostat-eta` | Mirostat 學習率 |
| DRY 取樣 | `--dry-*` | DRY 反重複取樣全套參數 |
| BNF 語法約束 | `--grammar-file` | 以 BNF 語法限制輸出格式 |
| JSON Schema | `--json-schema` | 以 JSON Schema 約束輸出結構 |
| 停止序列 | `--reverse-prompt` | 遇到指定文字時停止生成 |

### 8. 對話（LlamaChat）

| 功能 | 說明 |
|------|------|
| 系統提示詞 | 設定模型角色與行為 |
| 使用者訊息 | 當輪使用者輸入 |
| 對話歷史 | JSON 格式多輪對話記錄 |
| 歷史回饋 | 輸出更新後的對話歷史供下輪使用 |
| Jinja 模板 | 自動依模型格式化對話 |
| 自訂模板 | 指定聊天模板名稱 |
| 結構化輸出 | 支援 JSON Schema 約束回應格式 |
| 多模態對話 | 搭配 mmproj + 圖片進行視覺問答 |

### 9. 文字嵌入（LlamaEmbedding）

| 功能 | CLI 參數 | 說明 |
|------|----------|------|
| 嵌入計算 | llama-embedding | 將文字轉為向量表示 |
| 正規化類型 | `--embd-normalize` | 嵌入向量正規化方法 |
| 多筆分隔符 | `--embd-separator` | 批次處理多筆文字的分隔符 |

### 10. 程式碼填充（LlamaInfill）

| 功能 | CLI 參數 | 說明 |
|------|----------|------|
| 前綴 | `--in-prefix` | 填充位置之前的程式碼 |
| 後綴 | `--in-suffix` | 填充位置之後的程式碼 |
| FIM 模式 | `--infill` | 啟用 Fill-in-the-Middle |

### 11. 額外參數（Extra Args）

透過 `extra_args` 字串欄位傳入任何上述未覆蓋的 llama-cli 參數，例如：

| 參數 | 說明 |
|------|------|
| `--tensor-split` | 多 GPU 張量分割比例 |
| `--mlock` | 鎖定模型至記憶體 |
| `--reasoning-format` | 推理格式控制 |
| `--no-warmup` | 跳過預熱 |
| `--simple-io` | 簡化 IO 模式 |
| `--hf-repo` | 從 HuggingFace 下載模型 |

---

## 資料流類型

| 類型 | 說明 |
|------|------|
| CLI_CONFIG | LlamaCliConfig 輸出的配置 dict，包含二進位路徑、模型路徑、GPU 設定等 |
| STRING | 文字（提示詞、生成結果、JSON 等）|
| INT | 整數（exit_code 等）|
| BOOLEAN | 布林值（釋放狀態等）|
| IMAGE | ComfyUI 圖片 tensor（僅 LlamaImageEncoder 輸入）|

---

## 模型目錄

| 目錄 | 用途 |
|------|------|
| `ComfyUI/models/llama_gguf/` | 主模型 GGUF 檔案 |
| `ComfyUI/models/llama_mmproj/` | 視覺投射器 GGUF 檔案 |
