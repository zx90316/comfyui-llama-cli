[pytest]
testpaths = tests
norecursedirs = nodes utils .git __pycache__
addopts = --import-mode=importlib
markers =
    unit: Unit tests (no GPU, no model required)
    integration: Integration tests (requires GPU + llama-cli + GGUF model)
